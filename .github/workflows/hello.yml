name: Hello world

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: echo "Hello World!"
      - name: Time now is
        run: date
      - name: Directory content
        run: ls -l